<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8" />
    <title>Jogos</title>
    <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>
    <h1>Jogos Eletrônicos</h1>
    <p>
        <a href="/jogos/novo">Cadastrar novo jogo</a> | 
        <a href="/amigos">Amigos</a> | 
        <a href="/emprestimos">Empréstimos</a> |
        <a href="/api/jogos" target="_blank" style="color: #d9534f; font-weight: bold;">Exportar JSON (API)</a>
    </p>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Título</th>
                <th>Plataforma</th>
                <th>Dono</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
            <% jogos.forEach(j => { %>
            <tr>
                <td><%= j.id %></td>
                <td><%= j.titulo %></td>
                <td><%= j.plataforma %></td>
                <td><%= j.dono ? j.dono.nome : 'Sem dono' %></td>
                <td>
                    <a href="/jogos/detalhes/<%= j.id %>" style="margin-right: 10px;">Ver Detalhes</a>
                    
                    <a href="/jogos/editar/<%= j.id %>" style="margin-right: 10px;">Editar</a>
                    
                    <form action="/jogos/excluir/<%= j.id %>" method="POST" style="display:inline;">
                        <button type="submit">Excluir</button>
                    </form>
                </td>
            </tr>
            <% }) %> </tbody>
    </table> </body>
</html>